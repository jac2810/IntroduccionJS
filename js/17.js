//Diferencias entre Metodo y funcion

const numero1 = 20;
const numero2 = "20";

console.log(parseInt(numero2)); //La funcion es la que le siguen los parentesis "parseInt()"

console.log(numero1.toString()); //El metodo es el que tiene el nombre de la variable seguido por el punto "." y luego el nombre de la funcion. ".toString()"